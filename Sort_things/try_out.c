#include "stdafx.h"
#include <stdlib.h>
#include <time.h>
#include "sorts1.h"
//#include <string.h>
//#include <locale.h>


/*
void output(int c_arr[], int arr[], int len,int flag1, FILE *file) {
	printf("Default array :");
	print_arr(c_arr, NUM);
	printf("Sorted array :");
	print_arr(arr, NUM);
	if (file != NULL) {
		fprintf(file, "\n%d \n", n);
		if (flag1)
			for (int i = count; i < K; i++) {
				fprintf(file, "%s \t\t %d\n", sorts_arr[i].name, sorts_arr[i].time_sort);
				printf("%s\t %d \n", sorts_arr[i].name, sorts_arr[i].time_sort);
			}
		for (int i = 0; i < count; i++) {
			fprintf(file, "%s \t\t %d\n", sorts_arr[i].name, sorts_arr[i].time_sort);
			printf("%s\t %d \n", sorts_arr[i].name, sorts_arr[i].time_sort);
		}

	}

}*/